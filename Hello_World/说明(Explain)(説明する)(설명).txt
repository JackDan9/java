Java学习注意:
	1.Java语言拼写上严格区分大小写;
	2.一个Java源文件里面可以定义多个Java类,但其中最多只能有一个类被定义为Java类;
	3.若源文件中包括了public类,源文件必须和该public类同名
	4.一个源文件包含了N个Java类时,编译后会生成N份字节码文件,即每个类都会生成一份单独的class文件,
		且字节码文件名和其对应的类名相同.
编程建议:
	1.一个Java源程序只定义一个类,不同的类使用不同的源文件定义;
	2.将每个源文件中单独定义的类都定义为public的;
	3.保持Java源文件的主文件名与源文件中的类名一样;
	
English:
	Java study:
		1.Java language is spelled on the strict distinction between the size of the writing;
		2 A Java source file can be defined in a number of Java classes, but only one of them can be defined as a class Java;
		3 If the source file contains the public class, the source file must be the same name as the public class
		4. A source file contains n a Java class, the compiler will generate n a byte code file, 
			each class will generate a separate class file.
				And the bytecode file name and the class name.
	Programming suggestion:
		1 A Java source program only defines a class, different classes use different source file definitions;
		2 The class defined in each source file as public;
		3 Maintain master file Java source file name and file name in the source;
日本語:		
	Java学習注意：
		言葉に
		1.Javaスペルを厳格に区別されて、
		2 . 1人のJavaのソースファイルの中の多くのJavaクラス定義できるが、中にはせいぜい一人類の定義はJava類、
		3 .もしソースファイルにはpublicopinion類、ソースと同publicopinion類同名なければならない
		4 . 1つのソースファイルを含めたN Java品の洗濯時は、コンパイルして生成してN部のバイトコードファイル、
				つまりすべての類も会は1部の単独のクラスファイル生成、
					しかもバイトコードファイル名とその対応した一般名と同じ。
		プログラミングの提案：
			1.つのJavaのソースは定義の類、違う類によってソースファイル定義、
			2 .すべてのソースファイルで単独定義の類もpublicopinionの定義、
Javaのソースを
			3 .主ファイル名とソースファイルの中の一般名のように、
???:	
	?? ?? ??:
		1.Java ?? ??? ?? ??? ???? ??;
		2. ? ?? ?? ?? ?? ?? ? ? ?? ??? ? ???, ? ? ?? ?? ? ????? ??? ?? ?? ???;
		3. ?? ?? ?? ? ?? public ???, ?? ?? ??? ? ? public ??? ??
		4. ? ?? ?? ?? N ? ?? 类时, ??? ? ??? ?? ?? ?? N ?, ? ?? ??? ?? ? ?? ?? ?? class ??
			?? ??? ?? ?? ?? ? ? ?? 类名 ??.
	??: ?????
		1. ? ??? ?? ?? ???? ?? ? ??, ?? ??? ???? ?? ?? ?? ??;
		2. ? ?? ?? ?? ? ?? ?? ??? ? ??? ?? public;
		3. ?? ?? ?? ?? ??? ?? ?? ? ?? ??? 类名 ??;